# Agent B prompt

Use this in a fresh agent session opened at the `demo-agent-a` checkpoint:

> Continue task `3169a2b2`. Retrieve its saved Nool context and inspect the current code. Before editing, summarize the accepted requirements, completed work, and remaining work, and identify the Nool records you used. Then finish the task and verify every acceptance criterion.

Do not add the client's clarification to the prompt. Recovering it from Nool is the point of the demonstration.
